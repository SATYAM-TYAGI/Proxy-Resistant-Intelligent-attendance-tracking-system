PORT=5000
# Local: mongodb://localhost:27017/prats
# Atlas (cloud): paste the full SRV string from Atlas → Connect → Drivers. Host must look like cluster0.xxxxx.mongodb.net (not "123").
# Railway: use variable name MONGODB_URI (this app also reads MONGODB_URL if MONGODB_URI is empty).
MONGODB_URI=mongodb://localhost:27017/prats
JWT_SECRET=cfhjbvafjhbvsafkjbdskjfbdkjgb
AI_SERVICE_URL=http://127.0.0.1:8000
# When no admin exists in the DB, the API creates one on startup (no public signup).
# Override for production. Set SEED_DEFAULT_ADMIN=0 to skip seeding entirely.
SEED_ADMIN_EMAIL=admin@ddn.upes.ac.in
SEED_ADMIN_PASSWORD=admin@123
SEED_ADMIN_NAME=System Admin
